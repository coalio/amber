# Optional local ModernBERT attention scorer dependencies.
# Full installer mode fetches the CPU-only Torch wheel; source installs choose their own accelerator build.
torch==2.12.0
transformers==5.9.0
